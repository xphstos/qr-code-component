{
  "name": "qr-code-component",
  "version": "1.0.0",
  "description": "![Design preview for the QR code component coding challenge](./design/desktop-preview.jpg)",
  "author": "",
  "license": "ISC",
  "scripts": {
    "prestart": "rimraf .parcel-cache dist/",
    "start": "parcel --no-cache src/index.html",
    "serve": "http-server -p 1337 dist -c-1",
    "prebuild": "rimraf .parcel-cache dist/",
    "build": "parcel build src/index.html"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.5.0",
    "http-server": "^14.1.0",
    "parcel": "^2.5.0",
    "rimraf": "^3.0.2"
  }
}
